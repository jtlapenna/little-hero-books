# Cloudflare Pages configuration for marketing site
# This prevents Cloudflare from using the back-end wrangler.toml

name = "little-hero-labs"
pages_build_output_dir = "dist"
compatibility_date = "2024-11-01"

