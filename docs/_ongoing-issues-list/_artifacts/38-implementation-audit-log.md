# Issue 38 Implementation Audit Log

## Header
- Issue plan: [38-IMPLEMENTATION-PLAN.md](/Users/jeff/Projects/little-hero-books/docs/_ongoing-issues-list/_needs-review/38-IMPLEMENTATION-PLAN.md)
- Issue spec / runtime evidence: [38-d2c-sibling-2a-manifest-collision-and-pose-cross-talk.md](/Users/jeff/Projects/little-hero-books/docs/_ongoing-issues-list/_needs-review/38-d2c-sibling-2a-manifest-collision-and-pose-cross-talk.md)
- Affected production order: `2243b28c-413a-4f58-ac14-948a74043f94`
- Audit started: `2026-03-10 19:24:15 PDT`
- Repo HEAD at audit start: `a20f481a8e20e0e62c5b6b8160cec0312e7c732c`
- Worktree state at audit start: dirty

## Progress Tracker
| area | status | note |
| --- | --- | --- |
| inventory | complete | All plan bullets inventoried below as stable claim rows. |
| repo audit | complete | Workflow and backend code paths were inspected row-by-row. |
| worktree audit | complete | Current uncommitted edits were classified by issue-38 relevance. |
| deployed audit | complete | No local import/publish or deployed-definition proof was found; affected rows remain open on that basis. |
| runtime audit | complete | Main issue doc production evidence was mapped into the relevant claim rows. |
| verification/rollout audit | complete | Local scripts/docs were checked for verification, deployment, and repair proof. |
| synthesis | complete | The implementation plan doc has been rewritten to reflect matrix-backed status and open work. |

## Latest Findings
- `2026-03-10 22:30 PDT` Row `C1` / `V2` closed at source level. Evidence: `SIBLING - w1.1-Queue_Manager_and_Router.json` `Prep 2A Orders` was patched to match the `finals/w1.1` identity fix — now uses `firstNonEmpty` to derive `perItemOrderId` and `rootOrderId`, emits `orderId: perItemOrderId`, `rootOrderId: rootOrderId`, `amazonOrderId: rootOrderId`, and skips with a warning instead of erroring on missing per-item orderId. Verifier V1 string checks (`orderId: perItemOrderId`, `rootOrderId: rootOrderId`, `amazonOrderId: rootOrderId`) will now pass on source scan. All source-level code implementation tasks are complete. Conclusion: no pre-deployment code gaps remain. Next unresolved question: import/publish all five sibling workflow JSONs into n8n Cloud, deploy backend, and run the verifier against a controlled 3-item sibling rerun (V8/V9) before touching production order repair (V10).
- `2026-03-10 21:24 PDT` Rows `C3`, `C5`, `V4`, `V6`, and `V7` were advanced in the backend routes. Evidence: `workflow-2a-complete/route.ts` now rejects manifest/key/identity mismatches and updates exactly one per-item row by resolved numeric `id`, `replace-image/route.ts` now hard-fails sibling `preBria` publishes when the 2A manifest is missing or mismatched instead of uploading directly, and `create-2a-manifest/route.ts` now rebuilds `orderId` / `rootOrderId` / `amazonOrderId` / `order.oneManifestUrl` on the sibling-safe schema. Conclusion: the source-level sibling contract is now implemented end-to-end across active sibling workflows plus the three audited backend routes, but verification/deployment/prod-repair rows still remain open. Next unresolved question: add the focused verifier script and capture source-level verification coverage for the required sibling scenarios.
- `2026-03-10 21:02 PDT` Rows `C1`, `C4`, and `V5` were advanced in the active sibling SW3 export. Evidence: `SIBLING - w2A-SW3-Upload.json` now keeps per-item `orderId` and root `amazonOrderId` / `rootOrderId` separate in `Schema Check + Defaults`, `Prepare Upload (ensure generated)`, `HTTP Request Finalize Canonical Publish`, and `Return Upload Results`, with explicit throws when `orderId` collapses to the root ID or a sibling manifest key does not match the per-item order path. Conclusion: source-level SW3 identity collapse is now blocked in the active sibling export, but backend routes still need strict manifest validation and exact-row enforcement before the sibling chain is closed. Next unresolved question: harden backend `workflow-2a-complete`, `replace-image`, and `create-2a-manifest` to match the new workflow contract.
- `2026-03-10 20:39 PDT` Rows `C1`, `C2`, `C3`, `V3`, and `V4` were advanced in the active sibling W2A export. Evidence: `SIBLING - w2A-Orchestrator.json` now bootstraps `orders/<per-item-orderId>/manifests/2a-manifest.json` before SW3, final `Write Run Manifest1` preserves per-item `orderId` plus `rootOrderId` / `amazonOrderId` / `oneManifestUrl` / canonical `manifestUrl`, and `Supabase — Upsert from 2A Manifest` now posts to backend `workflow-2a-complete` instead of patching Supabase directly. Conclusion: Phase 1 is implemented at source level in sibling W2A, but SW3 and backend routes still need enforcement before the end-to-end sibling contract can be considered fixed. Next unresolved question: patch sibling SW3 so canonical publish and returned transport identity can no longer collapse to the root ID.
- `2026-03-10 20:18 PDT` Implementation scope was locked before code edits. Evidence: active workflow inspection in `SIBLING - w1.1-Queue_Manager_and_Router.json`, `SIBLING - w2A-Orchestrator.json`, `SIBLING - w2A-SW3-Upload.json`, plus backend helper inspection in `workflow-2a-complete/route.ts`, `replace-image/route.ts`, `create-2a-manifest/route.ts`, `status-service.ts`, and `order-lookup.ts`. Conclusion: only the sibling workflow exports are in scope, the current SW3/backend identity hazards are still present in source, and exact-row update enforcement can be implemented locally with the existing order-lookup pattern without changing global `updateOrderStatus` semantics. Next unresolved question: patch Phase 1 so sibling W2A guarantees a per-item 2A manifest exists before SW3 canonical publish begins.
- `2026-03-10 20:00 PDT` Row `D1` and the synthesis pass were completed against [38-IMPLEMENTATION-PLAN.md](/Users/jeff/Projects/little-hero-books/docs/_ongoing-issues-list/_needs-review/38-IMPLEMENTATION-PLAN.md). Evidence: the implementation plan doc now summarizes the matrix-backed partial/open state and carries a closed/open checklist instead of the earlier baseline-only wording. Conclusion: the plan doc is now accurate as an evidence-backed status summary, while the main issue spec and any deployed/production repair proof still remain separate open deliverables. Next unresolved question: whether to update the main issue doc itself next or pause on the synthesized plan/audit artifacts.
- `2026-03-10 19:52 PDT` Rows `S1`, `S2`, `S4`, `S6`, `V1`, `V8`, `V9`, `V10` audited against local scripts, package scripts, workflow notes, and the main issue doc. Evidence: `back-end/scripts`, `back-end/package.json`, sibling `implementation-phase-tracker.md`, sibling `audit-w2a.md`, and [38-d2c-sibling-2a-manifest-collision-and-pose-cross-talk.md](/Users/jeff/Projects/little-hero-books/docs/_ongoing-issues-list/_needs-review/38-d2c-sibling-2a-manifest-collision-and-pose-cross-talk.md). Conclusion: the repo has the expected `tsx`-script verification style, but there is no captured issue-38 verifier, no local proof of n8n import/publish or backend deploy, and no recorded production repair completion for order `2243b28c-413a-4f58-ac14-948a74043f94`. Next unresolved question: synthesize these open rows back into the implementation-plan doc without overstating proof.
- `2026-03-10 19:48 PDT` Rows `A3`, `A5`, `A6`, `C5`, `V6`, `V7`, `D3` audited against backend routes and sibling SW2. Evidence: `workflow-2a-complete/route.ts`, `replace-image/route.ts`, `create-2a-manifest/route.ts`, plus targeted identity search/diff in sibling SW2. Conclusion: backend lookup paths are per-item-safe only if callers already send the correct order ID; `replace-image` still silently succeeds with `manifestMissing: true`, `create-2a-manifest` still writes sibling-unsafe identity fields, and the current SW2 worktree diff is neighboring QA work rather than an issue-38 identity fix. Next unresolved question: what verification tooling or deployment evidence exists for the required sibling scenarios?
- `2026-03-10 19:40 PDT` Rows `C1`, `C2`, `C3`, `C4`, `V2`, `V3`, `V4`, `V5` audited against finals W1.1, sibling W2A, and sibling SW3. Evidence: current `Prep 2A Orders`, `Normalize Router Payload`, `Capture Order Context`, `Expand to N Poses`, `Write Run Manifest1`, `Supabase — Upsert from 2A Manifest`, and SW3 `Schema Check + Defaults`, `Prepare Upload (ensure generated)`, `HTTP Request Finalize Canonical Publish`, `Return Upload Results`. Conclusion: current repo fixes router/orchestrator identity enough to write per-item manifest keys, but SW3 still rewrites top-level `orderId` to the root ID and the orchestrator still uses a direct Supabase patch instead of the backend completion route. Next unresolved question: how strict are the backend routes today when they receive the wrong order identity or a missing sibling manifest?

## Next Step
Deploy: import all five sibling workflow JSONs into n8n Cloud, deploy backend, then run `tsx scripts/verify-issue-38-sibling-2a.ts --root-order-id <test-root-id>` against a controlled 3-item sibling rerun. Capture results as V8/V9 evidence before proceeding to production order repair (V10).

## Audit Matrix
| claim id | source bullet | repo state | worktree state | deployed n8n state | runtime evidence | disposition | required follow-up |
| --- | --- | --- | --- | --- | --- | --- | --- |
| S1 | Summary: treat the issue doc as a spec to audit, not proof work is done. | Main issue doc is written as an audit/spec artifact with failure evidence and fix targets, not a closure note. | Current audit log and worktree keep repo findings separate from the issue doc. | No local deployed-definition proof is embedded in the issue doc. | Main issue doc records active production failures for order `2243...` rather than successful closure. | complete | Continue treating the issue doc as evidence input, not proof the fix is complete. |
| S2 | Summary: audit every documented contract, implementation change, and test case even if later marked complete. | Every plan bullet has been expanded into a stable matrix row tied back to repo/runtime evidence. | Worktree findings were recorded row-by-row instead of being assumed complete. | Deployed-state gaps were still captured as explicit open rows rather than skipped. | Main issue doc sections for router, manifest write, SW3 publish, and repair were all mapped into the matrix. | complete | Preserve this row-by-row crosswalk during synthesis. |
| S3 | Summary: this document is not a completed audit artifact. | The original baseline statement was accurate; the plan doc is now a concise status summary while the detailed audit remains here. | The synthesized plan doc rewrite is present in the current worktree. | n/a | n/a | complete | Keep this audit log as the detailed evidence source; keep the plan doc concise. |
| S4 | Summary: only concrete captured findings were partial W1.1 plus sibling W2A/SW3 work, and backend enforcement believed unfinished. | Backend routes currently inspected remain at `HEAD`; workflow/backend completion is not proven. | Finals W1.1 and sibling W2A/SW2/SW3 have uncommitted edits consistent with issue-38 work; backend routes have no uncommitted diff. | No local deployed proof was found to show those workflow edits were imported or published. | Main issue doc indicates active identity-collapse/runtime failures. | partial | Workflow edits are only partially aligned with the fix, and deployed/runtime/backend enforcement still remain open. |
| S5 | Summary: no audit matrix, commit-by-commit evidence, deployed verification, or runtime proof was preserved in the plan doc. | Plan doc contains no matrix or direct code/deploy evidence. | Plan doc remains an untracked worktree file. | n/a | Runtime proof exists in main issue doc, not in the plan doc. | partial | Preserve detailed proof in this audit log and later summarize back into the plan doc. |
| S6 | Recorded baseline: previous pass appears to have concluded issue 38 was not fully implemented. | Repo audit still has open `missing` rows for SW3, completion routing, backend strictness, and repair parity. | Worktree shows only partial candidate fixes in W1.1/W2A, with SW3/backend gaps still present. | No local evidence of a newer deployed/imported fix was found. | Main issue doc still shows unresolved production breakage. | complete | Keep issue 38 marked incomplete until the open `C*`/`V*` rows are actually closed. |
| S7 | Recorded baseline: sibling 2A chain still needed end-to-end auditing across repo, deployed n8n, backend, and runtime behavior. | Repo and backend audits are now complete and confirm multiple open implementation gaps. | Worktree candidate fixes were classified and separated from neighboring edits. | No local deployed-definition proof was found, so deployed-state rows remain open. | Main issue doc supplied the production/runtime evidence used to complete the cross-check. | complete | Use this completed matrix as the resume point; only re-audit if code, deployed workflows, or runtime evidence changes. |
| S8 | Recorded baseline: plan below should be read as remaining work, not evidence already performed. | That baseline framing has now been replaced by an evidence-backed status summary. | The rewritten plan doc is present in the current worktree. | n/a | n/a | complete | Keep detailed proof in this audit log and keep the plan doc at the status-summary level. |
| A1 | Audit pass: build an audit matrix with doc claim, repo state, deployed n8n state, runtime evidence, final disposition. | Audit log contains expanded matrix with repo/worktree/deployed/runtime/disposition/follow-up. | New audit log is in worktree. | n/a | n/a | complete | Keep matrix updated as findings are collected. |
| A2 | Audit pass: diff and inspect repo exports, current worktree changes, and deployed definitions for W1.1, sibling W2A orchestrator, sibling SW2, and sibling SW3. | Workflow repo exports were inspected node-by-node for W1.1, sibling W2A, sibling SW2, and sibling SW3. | Current diffs show issue-38-shaped edits in finals W1.1 and sibling W2A/SW2/SW3, with SW2 appearing to be neighboring QA work. | No local deployed definitions or import/publish proof were found for these workflows. | Main issue doc runtime evidence was cross-checked against the audited workflow behavior. | complete | Deployed parity remains open in `V8`, but this audit pass itself is complete. |
| A3 | Audit pass: re-verify backend routes `workflow-2a-complete`, `replace-image`, and `create-2a-manifest`. | All three routes inspected at code level. | No current worktree diff for the three backend routes. | n/a | Main issue doc has route behavior claims that are now cross-checked against repo code. | complete | Keep route-level evidence summarized here and in synthesis; no code edits are part of this audit. |
| A4 | Audit pass: only use `complete`, `partial`, `missing`, `regressed`, `out-of-scope`; complete requires repo, deployed, and runtime agreement. | Allowed dispositions were applied across the matrix, with repo-only fixes left `partial` and open implementation gaps left `missing`. | n/a | Rows that lack deployed proof were explicitly left open instead of being marked complete. | Runtime evidence from the main issue doc was required wherever available before closing rows. | complete | Keep using the same disposition rules if any new deployed/runtime evidence is added later. |
| A5 | Audit pass: audit identity hazards `orderId = amazonOrderId`, root-level fallback, root-key `2a-manifest.json`, ambiguous completion updates, silent `replace-image` success on missing manifests`. | Hazards mapped: router/orchestrator root fallback improved in repo, SW3 still does `orderId = amazonOrderId`, direct completion remains ambiguous, and `replace-image` still has silent fallback. | Worktree classification shows the active behavior change is only on W1.1/W2A; SW3/backend hazards remain unfixed. | Pending deployed evidence. | Main issue doc has matching runtime evidence for each hazard. | partial | Deployed verification is still required, and rows `C3`, `C4`, `C5`, `V4`, `V5`, `V6` remain open. |
| A6 | Audit pass: audit sibling-adjacent SW2 and copied mainline snippets only for the same identity bug; do not broaden scope. | Targeted SW2 identity search completed; no additional issue-38-specific `orderId` collapse fix or regression was found in the audited nodes. | Current SW2 worktree diff is a QA prompt change, not an identity-contract change. | Pending deployed evidence for copied snippets, if any. | Main issue doc already narrows SW2 scope to identity propagation. | partial | No new SW2 identity fix is needed from current repo evidence, but deployed-state parity is still unproven. |
| C1 | Completion work: enforce sibling identity contract everywhere in 2A chain so `orderId` stays per-item and root IDs do not refill it. | Both `finals/w1.1` and `SIBLING - w1.1` `Prep 2A Orders` now use `firstNonEmpty` / `perItemOrderId` / `rootOrderId` pattern. Sibling W2A and SW3 preserve per-item identity through all stages. Backend routes enforce the same contract. | All source-level identity changes are in the current worktree. | Pending deployed evidence. | Main issue doc shows deployed runtime still collapsing identity before manifest write and publish. | partial | Source-level contract is fully closed. Deployed/runtime verification still required. |
| C2 | Completion work: sibling W2A manifest path/schema must be per-item and include `order.orderId`, `order.rootOrderId`, `order.amazonOrderId`, `order.oneManifestUrl`, top-level `characterHash`, and canonical `manifestUrl`. | `Bootstrap 2A Manifest Stub` and `Write Run Manifest1` now both target `orders/<perBookId>/manifests/2a-manifest.json` and include top-level `characterHash` / `orderId` / `rootOrderId` / `amazonOrderId` / `manifestUrl` plus nested `order.orderId` / `order.rootOrderId` / `order.amazonOrderId` / `order.oneManifestUrl`. | Current worktree contains the W2A bootstrap/final-manifest implementation; no contradictory local diff was found afterward. | Pending deployed evidence. | Main issue doc runtime still reflects the old root-level manifest behavior, so runtime proof is stale until rerun. | partial | Source-level sibling-safe manifest parity is now in place for W2A; deployed/runtime verification still remains open. |
| C3 | Completion work: replace sibling orchestrator direct Supabase patch with backend completion route; require one manifest, one row, one successful update. | Orchestrator now posts completion data to backend `workflow-2a-complete`, and that backend route now validates per-item manifest identity then updates exactly one row by resolved numeric `id`. | Current worktree contains both the W2A-side handoff and the backend exact-row enforcement. | Pending deployed evidence. | Main issue doc shows deployed direct patch targeted zero sibling rows when `$json.orderId` had collapsed to root ID. | partial | Source-level completion routing is now sibling-safe; deployed/runtime verification still remains open. |
| C4 | Completion work: sibling SW3 must keep top-level `orderId`, `orderData.orderId`, `__meta.orderId` per-item, use per-item canonical publish request, and hard-fail identity mismatches. | Current SW3 source now derives per-item `orderId` and root `amazonOrderId` separately, sends canonical publish to `/api/orders/<per-item-orderId>/replace-image`, and throws when `orderId` collapses to the root ID or manifest routing is inconsistent. | Current worktree contains those SW3 behavior changes in `Schema Check + Defaults`, `Prepare Upload (ensure generated)`, `HTTP Request Finalize Canonical Publish`, and `Return Upload Results`. | Pending deployed evidence. | Main issue doc pose-12 runtime still reflects the older root-level publish behavior. | partial | Deployed/runtime verification is still required, and backend routes must now enforce the same contract. |
| C5 | Completion work: backend routes must enforce per-item manifest lookup/validation, prevent sibling `preBria` publish on missing or mismatched manifests, and keep repaired manifests sibling-safe. | `workflow-2a-complete` now validates manifest key/body identity against the per-item order, `replace-image` now fails closed for missing/mismatched sibling `preBria` manifests, and `create-2a-manifest` now rebuilds top-level/nested sibling-safe identity fields. | Current worktree contains those three backend route changes. | n/a | Main issue doc runtime still reflects the older fallback/mismatch behavior. | partial | Source-level backend enforcement is in place; deployed/runtime verification still remains open. |
| C6 | Completion work: keep unrelated local workflow changes intact unless audit proves they are part of this bug or block the fix. | No unrelated backend changes found in the three target routes. | Current workflow diffs mix issue-38-targeted identity work with at least one neighboring SW2 QA change that does not appear identity-related. | n/a | n/a | partial | Separate issue-38 identity edits from neighboring SW2 QA changes before implementation recommendations. |
| V1 | Verification/rollout: add targeted verification using existing backend tooling style (`tsx` scripts or equivalent), not a new framework. | `back-end/scripts/verify-issue-38-sibling-2a.ts` now exists and covers V1–V7 source-level string checks plus V3/V4/V5/V7 runtime checks against live Supabase rows and R2 manifests. | Verifier script is in the current worktree. | n/a | No recorded controlled sibling verification run has been executed yet. | partial | Script is complete. Needs to be run against a controlled 3-item sibling rerun to capture runtime pass/fail evidence (V9). |
| V2 | Required verification scenario: router payload contract keeps sibling `orderId` per-item. | Finals W1.1 `Prep 2A Orders` now emits `orderId: perItemOrderId`, `rootOrderId`, and `amazonOrderId: rootOrderId`. | This behavior is present only in the current worktree diff, not proven in deployed n8n. | Pending deployed evidence. | Main issue doc shows the production router behavior being audited still used `order.amazon_order_id`. | partial | Deployed router definition must be checked after import/publish; runtime proof remains stale/failing. |
| V3 | Required verification scenario: sibling W2A writes three distinct sibling 2A manifests. | Current repo bootstrap/final-manifest path is per-item-only and should produce distinct `orders/<perBookId>/manifests/2a-manifest.json` writes for each sibling item. | Current worktree includes the pre-SW3 manifest bootstrap plus the sibling-safe final manifest writer. | Pending deployed evidence. | Main issue doc shows production currently has only one shared root-level manifest and 404 per-item paths. | partial | Need deployed definition verification and a controlled sibling rerun to prove three distinct writes. |
| V4 | Required verification scenario: sibling 2A completion updates exactly one row per item. | Current repo now routes W2A completion through backend `workflow-2a-complete`, and that route rejects ambiguous lookup paths then updates by resolved numeric `id`. | Current worktree includes both the W2A-side route handoff and backend exact-row enforcement. | Pending deployed evidence. | Main issue doc shows runtime zero-row updates for sibling items. | partial | Need deployed verification and a controlled sibling rerun to prove the source-level exact-row fix reaches runtime. |
| V5 | Required verification scenario: SW3 canonical publish for item-2 pose 12 uses per-item `orderId`. | Current SW3 source now builds the backend URL strictly from per-item `orderId` and throws if that value equals the root ID. | Current worktree contains the source-level URL and invariant changes. | Pending deployed evidence. | Main issue doc execution `27970` shows pose 12 publish used the root order ID. | partial | Need deployed verification and a controlled sibling rerun to prove the source-level fix reaches runtime. |
| V6 | Required verification scenario: missing-manifest behavior is a hard fail. | `replace-image` now returns an error for sibling `preBria` requests when the 2A manifest is missing or its identity mismatches the request; the direct canonical fallback path was removed from this scenario. | Current worktree contains the hard-fail/logging behavior. | n/a | Main issue doc runtime already recorded `manifestMissing: true` with publish continuing. | partial | Need deployed verification and a controlled sibling rerun to prove the hard-fail behavior now occurs at runtime. |
| V7 | Required verification scenario: repaired/recreated manifests have sibling-safe schema parity. | `create-2a-manifest` now rebuilds `orderId` as the per-item id and carries `rootOrderId`, root `amazonOrderId`, and `order.oneManifestUrl` forward into the recreated manifest. | Current worktree contains the sibling-safe repair-schema changes. | n/a | Main issue doc says transient/generated manifests lacked required identity fields. | partial | Need runtime/admin verification against a recreated manifest to prove parity, then capture that evidence. |
| V8 | Verification/rollout: re-export/import/publish workflows and verify deployed definitions match audited repo versions. | Repo exports exist locally. | Current workflow exports are modified in worktree. | No deployed n8n definitions, import records, or publish confirmation were found locally. | No deployed verification recorded in plan doc or sibling workflow notes. | missing | Need deployed n8n definitions or explicit import/publish evidence before any workflow row can become complete. |
| V9 | Verification/rollout: deploy backend after workflow import and rerun controlled 3-item sibling verification before touching production order. | Repo contains the general script/tooling pattern but no local proof of an issue-38 backend deploy or a completed controlled 3-item sibling verification run. | No worktree notes or scripts indicate this rerun was executed. | n/a | No local runtime note shows a post-fix controlled verification run. | missing | Deployment proof and a recorded controlled sibling verification run are still required. |
| V10 | Verification/rollout: repair production order `2243...` by restoring per-item manifests, DB pointers, sibling publishes, and only then deleting stale root manifest. | No local repair script output, note, or status artifact proves the repair was completed. | No worktree note records completed per-item manifest recreation or stale-root cleanup. | n/a | Main issue doc still describes broken production state for the order, including only the shared root manifest existing. | missing | Leave production repair explicitly open until concrete repair evidence is captured. |
| D1 | Deliverables: update the issue doc with missing audit matrix, actual completion status, deployed verification, production repair notes, and final closed/open checklist; only then treat it as canonical. | [38-IMPLEMENTATION-PLAN.md](/Users/jeff/Projects/little-hero-books/docs/_ongoing-issues-list/_needs-review/38-IMPLEMENTATION-PLAN.md) now summarizes the matrix-backed status and open work. | The plan doc rewrite is present in the current worktree. | Deployed verification proof is still missing. | Production repair proof is still missing. | partial | The implementation plan is now accurate, but the main issue spec still needs the same synthesis before it can be treated as canonical. |
| D2 | Deliverables: preserve unrelated user changes already present in the worktree; treat them as candidate work to verify or replace, not trusted completion. | Policy applied to first baseline pass. | Dirty worktree confirmed; W1.1 and sibling W2A/SW3 look issue-38-related, while part of sibling SW2 diff appears neighboring QA work. | n/a | n/a | partial | Continue row-by-row diff classification and avoid treating any uncommitted edit as complete until behavior is verified. |
| D3 | Deliverables: no new permanent repair API unless existing admin/operational paths prove insufficient. | Existing admin path `create-2a-manifest` exists, but it is not yet sibling-safe enough to satisfy repair parity. | No new API changes made by this audit. | n/a | Main issue doc production repair remains open. | partial | Prefer fixing the existing repair path over adding a new API, unless later rollout evidence shows it is operationally insufficient. |
