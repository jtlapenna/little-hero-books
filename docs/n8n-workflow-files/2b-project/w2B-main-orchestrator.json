{
  "name": "w2B-main-orchestrator",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "bg-removal-v2",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "68c0ca81-869f-4af4-8aa8-f13b1aa4d5b0",
      "name": "Webhook Trigger",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1,
      "position": [
        -1680,
        -208
      ],
      "webhookId": "41599fba-eff6-48b2-bc4a-60e2c3acecf8"
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "{ \"status\": \"accepted\" }",
        "options": {
          "responseCode": 200
        }
      },
      "id": "05255d6e-e814-4df6-845d-1ee2a28bd116",
      "name": "Respond to Webhook (Ack)",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.4,
      "position": [
        -1456,
        -208
      ]
    },
    {
      "parameters": {
        "jsCode": "/**\n * Normalize 2B orchestrator input (JSON-only)\n */\nconst input = $json || {};\nconst body = input.body && input.body !== input ? input.body : input;\n\nconst pick = (...v) => v.find(x => x !== undefined && x !== null && x !== '');\n\nconst orderId = pick(input.orderId, input.amazonOrderId, body.orderId, body.amazonOrderId, null);\nif (!orderId) throw new Error('2B main: orderId is required');\n\n// 2A manifest URL\nlet manifestUrl = pick(input.manifestUrl, input.originalManifestUrl, body.manifestUrl, body.originalManifestUrl, null);\nif (!manifestUrl) {\n  const base = pick(input.backendUrl, body.backendUrl, 'https://admin.littleherolabs.com');\n  manifestUrl = `${base}/api/manifests/book-mvp-simple-adventure/orders/${String(orderId)}/manifests/2a-manifest.json`;\n}\n\nconst backendUrl = pick(input.backendUrl, body.backendUrl, 'https://admin.littleherolabs.com');\nconst callbackUrl = pick(input.callbackUrl, body.callbackUrl, `${backendUrl}/webhook/2b-callback`);\n\n// n8n Cloud-friendly: pass sw1 workflow id in the request payload (or set it directly in the Execute Workflow node UI)\nconst sw1WorkflowId = pick(input.sw1WorkflowId, body.sw1WorkflowId, null);\n\nconst batchSize = Math.max(1, Math.min(3, Number(pick(input.batchSize, body.batchSize, 1)) || 1));\nconst force = !!pick(input.force, body.force, false);\n\nreturn [{ json: { orderId, backendUrl, manifestUrl, callbackUrl, sw1WorkflowId, batchSize, force } }];\n"
      },
      "id": "29a13a8f-09a7-4606-be94-fb0c0a2807f6",
      "name": "Normalize 2B Input",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -1232,
        -208
      ]
    },
    {
      "parameters": {
        "url": "={{ $json.manifestUrl }}{{ $json.manifestUrl.includes('?') ? '&' : '?' }}v={{ Date.now() }}",
        "options": {
          "response": {
            "response": {
              "responseFormat": "json"
            }
          },
          "timeout": 90000
        }
      },
      "id": "4fec6b19-c908-422a-b719-67a0016c5936",
      "name": "Download 2A Manifest",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        -992,
        -208
      ]
    },
    {
      "parameters": {
        "url": "={{ $items('Normalize 2B Input')[0].json.backendUrl + '/api/manifests/book-mvp-simple-adventure/orders/' + $items('Normalize 2B Input')[0].json.orderId + '/manifests/2b-manifest.json?v=' + Date.now() }}",
        "options": {
          "response": {
            "response": {
              "responseFormat": "json"
            }
          },
          "timeout": 90000
        }
      },
      "id": "372abdd5-a408-4e94-aec1-17766de61944",
      "name": "Download 2B Manifest (optional)",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        -752,
        -208
      ]
    },
    {
      "parameters": {
        "jsCode": "/**\n * Build pose worklist (idempotent + replacement-aware)\n * Async mode: we only trigger s2B-sw1 and return.\n */\nconst manifest2a = $json || {};\nif (!manifest2a.schema) throw new Error('2B main: expected manifest JSON from Download 2A Manifest');\n\n// Recover context from Normalize node (HTTP nodes overwrite input)\nlet ctx = {};\ntry { ctx = $items('Normalize 2B Input', 0, $runIndex)?.[0]?.json || {}; } catch {}\n\nconst orderId = ctx.orderId || manifest2a.order?.amazonOrderId || manifest2a.order?.orderId;\nconst characterHash = manifest2a.characterHash || manifest2a.order?.characterHash || null;\nif (!orderId) throw new Error('2B main: orderId missing after manifest download');\nif (!characterHash) throw new Error('2B main: characterHash missing');\n\nconst approved = (manifest2a.entries || []).filter(e => e?.approved && e?.status === 'approved');\nif (!approved.length) return [{ json: { orderId, characterHash, scheduled: 0, reason: 'NO_APPROVED_POSES' } }];\n\n// Existing 2B manifest (optional) from the previous node\nlet existing2b = null;\ntry {\n  const maybe = $items('Download 2B Manifest (optional)', 0, $runIndex)?.[0]?.json;\n  if (maybe && maybe.schema === 'lhb.run-manifest@v2.0') existing2b = maybe;\n} catch {}\n\nconst existingEntries = Array.isArray(existing2b?.entries) ? existing2b.entries : [];\nconst existingByPose = new Map(existingEntries.map(e => [Number(e.poseNumber), e]));\n\n// NOTE: existing 2B manifest lookup occurs in callback for MVP to keep orchestrator lean\n// Idempotency can still be applied in callback using sourceApprovedKey/sourceReplacedAt.\n\nconst backendUrl = ctx.backendUrl || 'https://admin.littleherolabs.com';\nconst callbackUrl = ctx.callbackUrl;\nconst force = !!ctx.force;\n\nconst work = approved\n  .filter(p => {\n    if (force) return true;\n    const poseNum = Number(p?.poseNumber);\n    \n    // Check 2A manifest for existing Bria status (prevent duplicate submissions)\n    const hasBriaStatus = !!(p.briaStatusUrl || p.briaRequestId);\n    if (hasBriaStatus) {\n      console.log(`✓ Pose ${poseNum}: Already has Bria status, skipping submission`);\n      return false; // Skip - already submitted\n    }\n    \n    // Existing 2B manifest check (keep as-is)\n    const ex = existingByPose.get(poseNum);\n    if (!ex) return true;\n    const srcKey = p.approvedKey || null;\n    const srcReplacedAt = p.replacedAt || null;\n    const srcReplacementCount = Number(p.replacementCount || 0);\n    const done = !!(\n      ex.bgRemovedKey &&\n      String(ex.briaStatus || '').toLowerCase() === 'completed' &&\n      (ex.sourceApprovedKey || null) === srcKey &&\n      ((ex.sourceReplacedAt || null) === srcReplacedAt || Number(ex.sourceReplacementCount || 0) === srcReplacementCount)\n    );\n    return !done;\n  })\n  .map(p => ({\n  orderId,\n  characterHash,\n  poseNumber: p.poseNumber,\n  approvedKey: p.approvedKey,\n  replacedAt: p.replacedAt || null,\n  replacementCount: Number(p.replacementCount || 0),\n  manifest2aUrl: ctx.manifestUrl || null,\n  backendUrl,\n  callbackUrl,\n  force\n}));\n\n// Emit one item per pose for SplitInBatches\nreturn work.map(w => ({ json: w }));\n"
      },
      "id": "a16703a6-533a-4d9b-ad60-6b83aaf810f2",
      "name": "Build Worklist",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -512,
        -208
      ]
    },
    {
      "parameters": {
        "batchSize": "={{ $items('Normalize 2B Input')[0].json.batchSize }}",
        "options": {}
      },
      "id": "9fb4e241-8dd3-48c7-bb2a-9a250a8a5e47",
      "name": "Split In Batches",
      "type": "n8n-nodes-base.splitInBatches",
      "typeVersion": 3,
      "position": [
        -272,
        -208
      ]
    },
    {
      "parameters": {
        "source": "url",
        "workflowUrl": "https://thepeakbeyond.app.n8n.cloud/workflow/qmk3cTggZlR2WQHA",
        "options": {}
      },
      "id": "e32a6904-385f-4931-853c-ff64f0fdafab",
      "name": "Execute Workflow: s2B-sw1",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1,
      "position": [
        -32,
        -320
      ]
    },
    {
      "parameters": {
        "jsCode": "/**\n * Final response payload (does not wait for workers).\n */\nconst items = $items('Build Worklist', 0, $runIndex) || [];\nlet ctx = {};\ntry { ctx = $items('Normalize 2B Input', 0, $runIndex)?.[0]?.json || {}; } catch {}\nreturn [{ json: { orderId: ctx.orderId, scheduled: items.length, callbackUrl: ctx.callbackUrl, batchSize: ctx.batchSize } }];\n"
      },
      "id": "86de4dfc-fa04-42fd-a780-d9fe7cd3dacc",
      "name": "Final Summary",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -32,
        -64
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $json.backendUrl || 'https://admin.littleherolabs.com' }}/api/webhooks/workflow-2b-submitted",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Authorization",
              "value": "={{ 'Bearer ' + ($env.BACKEND_API_TOKEN || '') }}"
            },
            {
              "name": "Content-Type",
              "value": "application/json"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ { orderId: $json.orderId, scheduledPoses: $json.scheduled, batchSize: $json.batchSize, timestamp: new Date().toISOString() } }}",
        "options": {
          "response": {
            "response": {
              "responseFormat": "json"
            }
          },
          "timeout": 90000
        }
      },
      "id": "f42322af-0ca0-42bd-81c9-b9015b9001da",
      "name": "Notify Backend: BRIA_READY",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        208,
        -64
      ]
    }
  ],
  "pinData": {},
  "connections": {
    "Webhook Trigger": {
      "main": [
        [
          {
            "node": "Respond to Webhook (Ack)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Respond to Webhook (Ack)": {
      "main": [
        [
          {
            "node": "Normalize 2B Input",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalize 2B Input": {
      "main": [
        [
          {
            "node": "Download 2A Manifest",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Download 2A Manifest": {
      "main": [
        [
          {
            "node": "Download 2B Manifest (optional)",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Worklist": {
      "main": [
        [
          {
            "node": "Split In Batches",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Split In Batches": {
      "main": [
        [
          {
            "node": "Execute Workflow: s2B-sw1",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Final Summary",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Download 2B Manifest (optional)": {
      "main": [
        [
          {
            "node": "Build Worklist",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Final Summary": {
      "main": [
        [
          {
            "node": "Notify Backend: BRIA_READY",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1"
  },
  "versionId": "5a72cc64-43d7-41c8-a008-8ca0b0416828",
  "meta": {
    "instanceId": "010748b7a1466c46dced3f8b2bdbc3bc174722f0672e5a4c9529354f5ff306f8"
  },
  "id": "2YBbqAQ70h9j0u1I",
  "tags": []
}