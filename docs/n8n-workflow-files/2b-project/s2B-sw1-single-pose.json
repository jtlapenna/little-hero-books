{
  "name": "s2B-sw1-single-pose",
  "nodes": [
    {
      "parameters": {
        "jsCode": "/**\n * s2B-sw1: single-pose Bria background removal\n * Input: one pose item from 2B main\n */\nconst j = $json || {};\nconst pick = (...v) => v.find(x => x !== undefined && x !== null && x !== '');\n\nconst orderId = pick(j.orderId, null);\nconst characterHash = pick(j.characterHash, null);\nconst poseNumberRaw = pick(j.poseNumber, null);\nconst poseNumber = (poseNumberRaw === 0 || poseNumberRaw === '0') ? 0 : Number(poseNumberRaw);\nconst approvedKey = pick(j.approvedKey, null);\n\nif (!orderId) throw new Error('s2B-sw1: orderId required');\nif (!characterHash) throw new Error('s2B-sw1: characterHash required');\nif (!Number.isFinite(poseNumber)) throw new Error('s2B-sw1: poseNumber required');\nif (!approvedKey) throw new Error('s2B-sw1: approvedKey required');\n\nconst backendUrl = pick(j.backendUrl, 'https://admin.littleherolabs.com');\nconst callbackUrl = pick(j.callbackUrl, `${backendUrl}/webhook/2b-callback`);\n\nconst replacedAt = pick(j.replacedAt, null);\nconst replacementCount = Number(pick(j.replacementCount, 0) || 0);\nconst manifest2aUrl = pick(j.manifest2aUrl, null);\n\n// Build a replacement-aware cache-busted source URL via backend proxy (private bucket-safe)\nconst replacedAtMs = replacedAt ? Date.parse(String(replacedAt)) : NaN;\nconst v = Number.isFinite(replacedAtMs) ? replacedAtMs : (replacementCount > 0 ? replacementCount : Date.now());\nconst sourceUrl = `${backendUrl}/api/assets/${approvedKey}?v=${v}`;\n\nreturn [{ json: {\n  orderId, characterHash, poseNumber, approvedKey,\n  backendUrl, callbackUrl,\n  replacedAt, replacementCount, manifest2aUrl,\n  sourceUrl\n} }];\n"
      },
      "id": "normalize",
      "name": "Normalize Input",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -880,
        0
      ]
    },
    {
      "parameters": {
        "jsCode": "/**\n * Build Bria remove-bg request payload (JSON).\n * Uses env var BRIA_API_KEY in n8n host.\n */\nconst j = $json;\nconst apiKey = $env.BRIA_API_KEY;\nif (!apiKey) throw new Error('s2B-sw1: missing $env.BRIA_API_KEY');\n\n// Bria endpoint expects image_url and configuration (per existing 2B workflow)\nconst payload = {\n  image_url: j.sourceUrl,\n  sync: false\n};\n\nreturn [{ json: { ...j, bria: { apiKey, payload } } }];\n"
      },
      "id": "bria-payload",
      "name": "Build Bria Payload",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -640,
        0
      ]
    },
    {
      "parameters": {
        "url": "={{ $env.BRIA_REMOVE_BG_URL || 'https://api.bria.ai/v1/remove-background' }}",
        "method": "POST",
        "jsonParameters": true,
        "options": {
          "timeout": 90000,
          "response": {
            "response": {
              "responseFormat": "json"
            }
          }
        },
        "bodyParametersJson": "={{ JSON.stringify($json.bria.payload) }}",
        "headerParametersJson": "={{ JSON.stringify({ Authorization: 'Bearer ' + $json.bria.apiKey, 'Content-Type': 'application/json' }) }}"
      },
      "id": "bria-submit",
      "name": "Bria Submit",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        -400,
        0
      ]
    },
    {
      "parameters": {
        "jsCode": "/**\n * Extract Bria status URL/requestId and begin bounded polling loop state.\n */\nconst resp = $json || {};\nlet ctx = {};\ntry { ctx = $items('Normalize Input', 0, $runIndex)?.[0]?.json || {}; } catch {}\n\nconst statusUrl = resp.status_url || resp.statusUrl || resp.data?.status_url || null;\nconst requestId = resp.request_id || resp.requestId || resp.data?.request_id || null;\nif (!statusUrl && !requestId) {\n  throw new Error('s2B-sw1: Bria submit did not return status_url/request_id');\n}\n\nreturn [{ json: { ...ctx, briaStatusUrl: statusUrl, briaRequestId: requestId, attempt: 0 } }];\n"
      },
      "id": "extract",
      "name": "Extract Bria Tracking",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -160,
        0
      ]
    },
    {
      "parameters": {
        "amount": "={{ Math.min(90, 8 + Math.floor((Number($json.attempt || 0)) / 3) * 10) }}",
        "unit": "seconds"
      },
      "id": "wait",
      "name": "Wait",
      "type": "n8n-nodes-base.wait",
      "typeVersion": 1,
      "position": [
        80,
        0
      ]
    },
    {
      "parameters": {
        "url": "={{ $json.briaStatusUrl }}",
        "method": "GET",
        "options": {
          "timeout": 90000,
          "response": {
            "response": {
              "responseFormat": "json"
            }
          }
        },
        "headerParametersJson": "={{ JSON.stringify({ Authorization: 'Bearer ' + ($env.BRIA_API_KEY || '') }) }}"
      },
      "id": "bria-poll",
      "name": "Bria Poll",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        320,
        0
      ]
    },
    {
      "parameters": {
        "jsCode": "/**\n * Decide: continue polling vs completed vs failed/timeout.\n */\nconst poll = $json || {};\nlet ctx = {};\ntry { ctx = $items('Extract Bria Tracking', 0, $runIndex)?.[0]?.json || {}; } catch {}\n\nconst maxAttempts = Number($env.BRIA_MAX_POLL_ATTEMPTS || 20);\nconst attempt = Number(ctx.attempt || 0) + 1;\n\nconst status = (poll.status || poll.state || poll.data?.status || '').toLowerCase();\nconst resultUrl = poll.result_url || poll.resultUrl || poll.data?.result_url || poll.data?.resultUrl || null;\nconst errMsg = poll.error?.message || poll.message || poll.data?.error || null;\n\n// Normalize terminal states\nif (status === 'completed' || status === 'done' || (resultUrl && !status)) {\n  if (!resultUrl) throw new Error('s2B-sw1: completed but missing result_url');\n  return [{ json: { ...ctx, attempt, briaStatus: 'completed', briaResultUrl: resultUrl, briaErrorMessage: null } }];\n}\nif (status === 'failed' || status === 'error') {\n  return [{ json: { ...ctx, attempt, briaStatus: 'failed', briaResultUrl: null, briaErrorMessage: errMsg || 'Bria failed' } }];\n}\n\nif (attempt >= maxAttempts) {\n  return [{ json: { ...ctx, attempt, briaStatus: 'timeout', briaResultUrl: null, briaErrorMessage: `Timeout after ${maxAttempts} polls` } }];\n}\n\n// Continue\nreturn [{ json: { ...ctx, attempt, briaStatus: 'polling', briaResultUrl: null } }];\n"
      },
      "id": "decide",
      "name": "Decide Poll",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        560,
        0
      ]
    },
    {
      "parameters": {
        "conditions": {
          "string": [
            {
              "value1": "={{ $json.briaStatus }}",
              "operation": "equals",
              "value2": "polling"
            }
          ]
        }
      },
      "id": "if-poll",
      "name": "If Polling",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [
        780,
        0
      ]
    },
    {
      "parameters": {
        "conditions": {
          "string": [
            {
              "value1": "={{ $json.briaStatus }}",
              "operation": "equals",
              "value2": "completed"
            }
          ]
        }
      },
      "id": "if-complete",
      "name": "If Completed",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [
        1000,
        160
      ]
    },
    {
      "parameters": {
        "url": "={{ $json.briaResultUrl }}",
        "method": "GET",
        "options": {
          "timeout": 90000,
          "response": {
            "response": {
              "responseFormat": "file"
            }
          }
        }
      },
      "id": "download-result",
      "name": "Download BG Removed (Binary)",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1240,
        160
      ]
    },
    {
      "parameters": {
        "jsCode": "// Emits where to fetch the neon canvas from, and what to name its binary\nreturn [{\n  json: {\n    bgBucket: 'little-hero-assets',\n    bgKey: 'book-mvp-simple-adventure/backgrounds/transparency-qa/neon-background.png',\n    bgBinary: 'bg'\n  }\n}];\n"
      },
      "id": "qa-bg-key",
      "name": "QA: Key for Neon BG",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1400,
        320
      ]
    },
    {
      "parameters": {
        "operation": "download",
        "bucketName": "={{ $json.bgBucket }}",
        "fileKey": "={{ $json.bgKey }}",
        "binaryPropertyName": "={{ $json.bgBinary }}"
      },
      "id": "qa-bg-download",
      "name": "QA: Download Neon BG",
      "type": "n8n-nodes-base.s3",
      "typeVersion": 1,
      "position": [
        1640,
        320
      ]
    },
    {
      "parameters": {
        "operation": "composite",
        "dataPropertyName": "bg",
        "dataPropertyNameComposite": "data",
        "options": {}
      },
      "id": "qa-composite",
      "name": "QA: Composite BG + Character",
      "type": "n8n-nodes-base.editImage",
      "typeVersion": 1,
      "position": [
        1880,
        320
      ]
    },
    {
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "/**\n * Transparency QA Build Request \u2014 dual-image (composite + original) & filesystem-safe\n * Expects:\n *   - composite image under binary.bg  (from Edit Image \u2192 Composite base)\n *   - original cutout under binary.data (transparent PNG)\n * Emits:\n *   - json.geminiRequest (ready to POST)\n *   - drops binaries to keep memory low\n */\n\nconst idx = $itemIndex;\nconst bin = $input.item.binary || {};\n\nconst hasComposite = !!bin.bg;\nconst hasOriginal  = !!bin.data;\nconst compositeKey = hasComposite ? 'bg'   : Object.keys(bin)[0];\nconst originalKey  = hasOriginal  ? 'data' : Object.keys(bin)[1] || Object.keys(bin)[0];\n\nif (!compositeKey || !originalKey || !bin[compositeKey] || !bin[originalKey]) {\n  throw new Error('Expected binary.bg (composite) and binary.data (original) to be present.');\n}\n\nconst mimeComposite = bin[compositeKey].mimeType || 'image/png';\nconst mimeOriginal  = bin[originalKey].mimeType  || 'image/png';\n\nconst compBuf = await this.helpers.getBinaryDataBuffer(idx, compositeKey);\nconst origBuf = await this.helpers.getBinaryDataBuffer(idx, originalKey);\n\nconst compB64 = compBuf.toString('base64');\nconst origB64 = origBuf.toString('base64');\n\nconst qaPrompt = `\nTask: QA for accidental transparency holes *inside* the subject silhouette.\n\nYou are given two images:\n1) A composite image of the subject over a solid neon green background (#39FF14, RGB 57,255,20).\n2) The original PNG with a transparent background (has alpha channel).\n\nRules (be strict):\n- If ANY of the neon background color (#42f10c, \u00b15% tolerance) is visible *inside* the subject silhouette\n  (especially eyes/teeth/mouth/clothing/body), set \"needs_review\": true.\n- Use the original PNG's alpha to determine the silhouette. Any fully transparent area *not connected to the outer\n  transparent border* is an interior hole and must be flagged.\n- Ignore the outer green area around the figure and a 1\u20133 px antialias halo at the silhouette edge.\n- If uncertain, set \"needs_review\": true.\n- Be EXTRA careful when reviewing whites of the eyes and teeth.\n\nReturn strict JSON only (no markdown):\n{\"needs_review\": true|false, \"issues\": [\"...\"], \"confidence\": 0..1}\n`.trim();\n\nconst geminiRequest = {\n  contents: [{\n    role: \"user\",\n    parts: [\n      { text: qaPrompt },\n      { inline_data: { mime_type: mimeComposite, data: compB64 } },\n      { inline_data: { mime_type: mimeOriginal,  data: origB64 } }\n    ]\n  }],\n  generationConfig: {\n    response_mime_type: \"application/json\",\n    temperature: 0,\n    topP: 0\n  }\n};\n\nreturn {\n  json: {\n    ...$json,\n    qa: { ...( $json.qa || {} ), transparency: { requested: true, bgHex: \"#39FF14\" } },\n    geminiRequest\n  },\n  binary: {}\n};\n"
      },
      "id": "qa-build",
      "name": "QA: Build Gemini Request",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2120,
        320
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://generativelanguage.googleapis.com/v1beta/models/gemini-3-pro-image-preview:generateContent",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "googlePalmApi",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ $json.geminiRequest }}",
        "options": {
          "response": {
            "response": {
              "responseFormat": "json"
            }
          }
        }
      },
      "id": "qa-gemini",
      "name": "QA: Gemini Transparency",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        2360,
        320
      ]
    },
    {
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "/**\n * Parse Transparency QA Result (single item)\n * Recall-biased: fail if parse fails or confidence < 0.99\n */\nfunction extractText(resp) {\n  try {\n    const parts = resp?.candidates?.[0]?.content?.parts || [];\n    return parts.map(p => (p && typeof p.text === 'string' ? p.text : '')).join('\\n').trim();\n  } catch {\n    return '';\n  }\n}\n\nconst resp = $json || {};\nconst raw = extractText(resp);\n\nlet parsed = null;\ntry { parsed = raw ? JSON.parse(raw) : null; } catch { parsed = null; }\n\nconst needsReviewModel = parsed?.needs_review === true;\nconst confidence = Number.isFinite(parsed?.confidence) ? parsed.confidence : null;\n\nconst needsReview = needsReviewModel || !parsed || (confidence !== null && confidence < 0.99);\n\nconst issues = Array.isArray(parsed?.issues)\n  ? parsed.issues\n  : (raw ? [raw] : (needsReview ? ['Uncertain \u2014 manual check required'] : []));\n\nconst out = {\n  ...$json,\n  qa: {\n    ...($json.qa || {}),\n    transparency: { pass: !needsReview, issues, confidence }\n  },\n  needsReview,\n  reviewReason: needsReview ? 'transparency_fail_or_low_conf' : null,\n};\n\n// trim heavy fields\ndelete out.candidates; delete out.usageMetadata; delete out.modelVersion; delete out.responseId;\n\nreturn [{ json: out }];\n"
      },
      "id": "qa-parse",
      "name": "QA: Parse Result",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2600,
        320
      ]
    },
    {
      "parameters": {
        "jsCode": "/**\n * Prepare canonical R2 key and minimal metadata.\n */\nconst j = $json;\nconst poseNN = String(j.poseNumber).padStart(2,'0');\nconst bgRemovedKey = `book-mvp-simple-adventure/orders/${String(j.orderId)}/2b/pose${poseNN}_nobg.png`;\nreturn [{ json: { ...j, poseNN, bgRemovedKey, briaCompletedAt: new Date().toISOString() } }];\n"
      },
      "id": "prep-upload",
      "name": "Prep Upload",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1480,
        160
      ]
    },
    {
      "parameters": {
        "operation": "upload",
        "bucketName": "={{ $env.R2_ORDERS_BUCKET || 'little-hero-orders' }}",
        "binaryData": true,
        "binaryPropertyName": "data",
        "fileName": "={{ $json.bgRemovedKey }}",
        "additionalFields": {
          "contentType": "image/png"
        }
      },
      "id": "r2-upload",
      "name": "Upload BG Removed to R2",
      "type": "n8n-nodes-base.s3",
      "typeVersion": 1,
      "position": [
        1720,
        160
      ]
    },
    {
      "parameters": {
        "jsCode": "/**\n * Build result payload (JSON-only) to send to callback aggregator.\n */\nlet ctx = {};\ntry { ctx = $items('Normalize Input', 0, $runIndex)?.[0]?.json || {}; } catch {}\n\nconst isCompleted = $json.briaStatus === 'completed';\n\nconst result = {\n  orderId: ctx.orderId,\n  characterHash: ctx.characterHash,\n  poseNumber: ctx.poseNumber,\n  approvedKey: ctx.approvedKey,\n  sourceReplacedAt: ctx.replacedAt || null,\n  sourceReplacementCount: Number(ctx.replacementCount || 0),\n  sourceUrl: ctx.sourceUrl,\n  manifest2aUrl: ctx.manifest2aUrl || null,\n  briaStatus: $json.briaStatus,\n  briaRequestId: ctx.briaRequestId || null,\n  briaStatusUrl: ctx.briaStatusUrl || null,\n  briaCompletedAt: isCompleted ? ($json.briaCompletedAt || new Date().toISOString()) : null,\n  briaErrorMessage: isCompleted ? null : ($json.briaErrorMessage || null),\n  bgRemovedKey: isCompleted ? ($json.bgRemovedKey || null) : null,\n  bgRemovedContentType: isCompleted ? 'image/png' : null,\n  bgRemovedSizeBytes: null,\n  bgRemovedImageUrl: null,\n\n  // QA (transparency)\n  qa: ($json.qa && typeof $json.qa === 'object') ? $json.qa : null,\n\n  // Review flags: if QA sets needsReview/reviewReason, prefer those over bria-derived defaults\n  needsReview: ($json.needsReview === true) ? true : (!isCompleted),\n  reviewReason: ($json.needsReview === true)\n    ? ($json.reviewReason || 'transparency_fail_or_low_conf')\n    : (isCompleted ? null : ($json.briaStatus === 'timeout' ? 'bria_timeout' : 'bria_failed')),\n};\n\nreturn [{ json: { callbackUrl: ctx.callbackUrl, result } }];\n"
      },
      "id": "build-result",
      "name": "Build Callback Result",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1960,
        160
      ]
    },
    {
      "parameters": {
        "url": "={{ $json.callbackUrl }}",
        "method": "POST",
        "jsonParameters": true,
        "options": {
          "timeout": 90000,
          "response": {
            "response": {
              "responseFormat": "json"
            }
          }
        },
        "bodyParametersJson": "={{ JSON.stringify($json.result) }}",
        "headerParametersJson": "={{ JSON.stringify({ 'Content-Type': 'application/json' }) }}"
      },
      "id": "post-callback",
      "name": "POST to Callback",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        2200,
        160
      ]
    }
  ],
  "connections": {
    "Normalize Input": {
      "main": [
        [
          {
            "node": "Build Bria Payload",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Bria Payload": {
      "main": [
        [
          {
            "node": "Bria Submit",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Bria Submit": {
      "main": [
        [
          {
            "node": "Extract Bria Tracking",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Extract Bria Tracking": {
      "main": [
        [
          {
            "node": "Wait",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Wait": {
      "main": [
        [
          {
            "node": "Bria Poll",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Bria Poll": {
      "main": [
        [
          {
            "node": "Decide Poll",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Decide Poll": {
      "main": [
        [
          {
            "node": "If Polling",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "If Polling": {
      "main": [
        [
          {
            "node": "Wait",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "If Completed",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "If Completed": {
      "main": [
        [
          {
            "node": "Download BG Removed (Binary)",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Build Callback Result",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Download BG Removed (Binary)": {
      "main": [
        [
          {
            "node": "QA: Key for Neon BG",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Prep Upload": {
      "main": [
        [
          {
            "node": "Upload BG Removed to R2",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Upload BG Removed to R2": {
      "main": [
        [
          {
            "node": "Build Callback Result",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Callback Result": {
      "main": [
        [
          {
            "node": "POST to Callback",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "QA: Key for Neon BG": {
      "main": [
        [
          {
            "node": "QA: Download Neon BG",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "QA: Download Neon BG": {
      "main": [
        [
          {
            "node": "QA: Composite BG + Character",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "QA: Composite BG + Character": {
      "main": [
        [
          {
            "node": "QA: Build Gemini Request",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "QA: Build Gemini Request": {
      "main": [
        [
          {
            "node": "QA: Gemini Transparency",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "QA: Gemini Transparency": {
      "main": [
        [
          {
            "node": "QA: Parse Result",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "QA: Parse Result": {
      "main": [
        [
          {
            "node": "Prep Upload",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1"
  },
  "versionId": ""
}