# Active n8n-Centric Master Workflows

This folder is the active source of truth for the current n8n-centric workflow path.

Use these exports for:

- ongoing workflow edits
- re-imports into n8n during the current migration period
- the backward-compatible path that replaces the older `finals` set

Do not treat the legacy `finals` folder as the active edit target.
