# Legacy Finals Snapshot

This folder contains a snapshot of the committed JSON files from:

- [`docs/n8n-workflow-files/finals`](/Users/jeff/Projects/little-hero-books/docs/n8n-workflow-files/finals)

Snapshot date:

- `2026-03-18`

Purpose:

- preserve the older n8n-centric `finals` workflow set before sibling exports became the active working master set

Important caveat:

- this snapshot was taken from the repo's committed `HEAD` versions of the `finals` JSON files
- if a precise copy of the currently imported live n8n workflows is needed later, export them from n8n and archive that export separately
