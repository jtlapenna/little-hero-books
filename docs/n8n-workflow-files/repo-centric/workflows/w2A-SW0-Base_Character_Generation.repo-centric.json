{
  "name": "REPO - w2A-SW0-Base_Character_Generation",
  "nodes": [
    {
      "parameters": {
        "inputSource": "passthrough"
      },
      "id": "4bdbe4fa-fd76-46b9-a6c3-420d6727c3f4",
      "typeVersion": 1.1,
      "name": "When Executed by Another Workflow",
      "type": "n8n-nodes-base.executeWorkflowTrigger",
      "position": [
        -672,
        112
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{(($json.backendUrl || $json.orderContext?.backendUrl || 'https://admin.littleherolabs.com').replace(/\\/$/, '')) + '/api/internal/w2a/build-base-input'}}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Authorization",
              "value": "=Bearer REDACTED_BACKEND_API_TOKEN"
            },
            {
              "name": "Content-Type",
              "value": "application/json"
            }
          ]
        },
        "sendBody": true,
        "contentType": "raw",
        "rawContentType": "application/json",
        "body": "={{JSON.stringify($json.body && $json.body !== $json ? $json.body : $json)}}",
        "options": {
          "response": {
            "response": {
              "responseFormat": "json"
            }
          },
          "timeout": 90000
        }
      },
      "id": "841d3b8b-0ad7-41db-bdb0-4410abe0cd16",
      "name": "Fetch Repo W2A Base Input",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        -448,
        112
      ]
    },
    {
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "\nfunction asObject(value) {\n  return value && typeof value === 'object' && !Array.isArray(value) ? value : {};\n}\n\nfunction toBufferLike(value) {\n  if (!value) return null;\n  if (Buffer.isBuffer(value)) return value;\n  if (typeof value === 'string') return Buffer.from(value, 'utf8');\n  if (Array.isArray(value)) return Buffer.from(value);\n  const record = asObject(value);\n  if (record.type === 'Buffer' && Array.isArray(record.data)) return Buffer.from(record.data);\n  if (Array.isArray(record.data)) return Buffer.from(record.data);\n  return null;\n}\n\nfunction extractStreamText(value) {\n  const source = asObject(value);\n  const directOutBuffer = toBufferLike(source._outBuffer);\n  const outOffset = typeof source._outOffset === 'number' ? source._outOffset : null;\n  if (directOutBuffer && directOutBuffer.length > 0) {\n    const end = outOffset !== null && outOffset >= 0\n      ? Math.min(outOffset, directOutBuffer.length)\n      : directOutBuffer.length;\n    const text = directOutBuffer.subarray(0, end).toString('utf8');\n    if (text) return text;\n  }\n\n  const readableState = asObject(source._readableState);\n  const chunks = [];\n  const pushChunk = (chunk) => {\n    const buffer = toBufferLike(chunk);\n    if (buffer && buffer.length > 0) chunks.push(buffer);\n  };\n\n  const bufferState = readableState.buffer;\n  if (Array.isArray(bufferState)) {\n    for (const entry of bufferState) {\n      const record = asObject(entry);\n      pushChunk(entry);\n      pushChunk(record.data);\n      pushChunk(record.chunk);\n    }\n  } else {\n    const bufferList = asObject(bufferState);\n    let current = bufferList.head || null;\n    let guard = 0;\n    while (current && guard < 10000) {\n      const record = asObject(current);\n      pushChunk(record.data);\n      pushChunk(record.chunk);\n      current = record.next || null;\n      guard += 1;\n    }\n  }\n\n  if (!chunks.length) return null;\n  return Buffer.concat(chunks).toString('utf8');\n}\n\nfunction parseJsonPrefix(text) {\n  if (typeof text !== 'string') return null;\n  const start = text.search(/[\\[{]/);\n  if (start === -1) return null;\n\n  const stack = [];\n  let inString = false;\n  let escape = false;\n\n  for (let index = start; index < text.length; index += 1) {\n    const char = text[index];\n\n    if (inString) {\n      if (escape) {\n        escape = false;\n      } else if (char === '\\\\') {\n        escape = true;\n      } else if (char === '\"') {\n        inString = false;\n      }\n      continue;\n    }\n\n    if (char === '\"') {\n      inString = true;\n      continue;\n    }\n\n    if (char === '{' || char === '[') {\n      stack.push(char);\n      continue;\n    }\n\n    if (char === '}' || char === ']') {\n      const expected = char === '}' ? '{' : '[';\n      if (!stack.length || stack[stack.length - 1] !== expected) return null;\n      stack.pop();\n      if (!stack.length) {\n        try {\n          return JSON.parse(text.slice(start, index + 1));\n        } catch {\n          return null;\n        }\n      }\n    }\n  }\n\n  return null;\n}\n\nfunction parseMaybeJson(value) {\n  if (typeof value === 'string') {\n    try {\n      return JSON.parse(value);\n    } catch {\n      return parseJsonPrefix(value) ?? value;\n    }\n  }\n\n  const streamText = extractStreamText(value);\n  if (typeof streamText === 'string') {\n    try {\n      return JSON.parse(streamText);\n    } catch {\n      return parseJsonPrefix(streamText) ?? value;\n    }\n  }\n\n  return value;\n}\n\nfunction unwrapResponse(value) {\n  const parsed = parseMaybeJson(value);\n  const obj = asObject(parsed);\n  if (obj.success === true || obj.orderId || obj.characterHash || obj.baseCharacterKey) {\n    return obj;\n  }\n\n  for (const candidate of [obj.body, obj.data, obj.response, obj.result]) {\n    const nested = asObject(parseMaybeJson(candidate));\n    if (nested.success === true || nested.orderId || nested.characterHash || nested.baseCharacterKey) {\n      return nested;\n    }\n  }\n\n  return obj;\n}\n\nlet payload = unwrapResponse($json || {});\nif (!Object.keys(payload).length) {\n  try {\n    payload = unwrapResponse($items('Fetch Repo W2A Base Input', 0, $runIndex)?.[0]?.json || {});\n  } catch {}\n}\n\nif (!payload || typeof payload !== 'object' || Array.isArray(payload)) {\n  throw new Error('Could not parse W2A base input response');\n}\n\nif (payload.error && !payload.orderId && !payload.characterHash) {\n  throw new Error(payload.details || payload.error);\n}\n\nconst testMode = payload.testMode === true || payload.__testMode === true;\n\nreturn {\n  json: {\n    ...payload,\n    baseInputSource: 'repo',\n    __testMode: testMode,\n    testMode,\n    __meta: {\n      ...(payload.__meta || {}),\n      __testMode: testMode,\n    },\n  },\n  binary: $binary,\n};"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -224,
        112
      ],
      "id": "4673aa0f-417c-4f49-a86b-afe11f34bc4b",
      "name": "Schema Check + Defaults"
    },
    {
      "parameters": {
        "bucketName": "=little-hero-assets",
        "fileKey": "={{ $json.baseRefS3Key }}"
      },
      "type": "n8n-nodes-base.s3",
      "typeVersion": 1,
      "position": [
        0,
        0
      ],
      "id": "b51f8a16-92ef-496a-8543-ec51dce601d1",
      "name": "Load Base Character Image1",
      "credentials": {
        "s3": {
          "id": "7tJOX9QjL1jqyEjf",
          "name": "S3 account"
        }
      }
    },
    {
      "parameters": {
        "bucketName": "little-hero-assets",
        "fileKey": "={{$json.hairRefS3Key}}",
        "binaryPropertyName": "hair"
      },
      "id": "b6ea0358-d90a-49fa-b9cf-f8692dc54370",
      "name": "Load Hairstyle Reference (R2/S3)1",
      "type": "n8n-nodes-base.s3",
      "typeVersion": 1,
      "position": [
        0,
        224
      ],
      "credentials": {
        "s3": {
          "id": "7tJOX9QjL1jqyEjf",
          "name": "S3 account"
        }
      }
    },
    {
      "parameters": {
        "mode": "combine",
        "combinationMode": "mergeByPosition",
        "options": {}
      },
      "id": "9498b2e1-8176-427b-b7ac-9c5ac352dbc8",
      "name": "Merge Base & Hair Refs1",
      "type": "n8n-nodes-base.merge",
      "typeVersion": 2,
      "position": [
        224,
        112
      ]
    },
    {
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "\nconst j = { ...($json || {}) };\nconst b = $binary || {};\nconst itemIx = (typeof $itemIndex === 'number') ? $itemIndex : 0;\n\nfunction pickBinaryKey(map, candidates) {\n  for (const key of candidates) {\n    if (map[key]) return key;\n  }\n  return null;\n}\n\nasync function readBinaryAsInlineData(key, fallbackMime) {\n  const buffer = await this.helpers.getBinaryDataBuffer(itemIx, key);\n  return {\n    mimeType: b[key]?.mimeType || fallbackMime,\n    data: buffer.toString('base64'),\n  };\n}\n\nconst baseKey = pickBinaryKey(b, ['data', 'base', 'image', 'file', 'character']);\nif (!baseKey) {\n  throw new Error('Build Repo Gemini Base Request expected a base reference binary from Load Base Character Image1');\n}\n\nconst parts = [];\nparts.push({ text: j.userPromptText || '' });\nparts.push({ text: j.requestParts?.baseLabel || 'IMAGE A — BASE STYLE GUIDE.' });\nparts.push({ inlineData: await readBinaryAsInlineData(baseKey, 'image/png') });\n\nif (b.hair && j.hairRefS3Key) {\n  parts.push({ text: j.requestParts?.hairLabel || 'IMAGE B — HAIRSTYLE REFERENCE.' });\n  parts.push({ inlineData: await readBinaryAsInlineData('hair', 'image/png') });\n}\n\nif (b.skin && j.promptMeta?.hasSkinSwatch) {\n  parts.push({ text: j.requestParts?.skinLabel || 'IMAGE C — OPTIONAL SKIN SWATCH.' });\n  parts.push({ inlineData: await readBinaryAsInlineData('skin', 'image/png') });\n}\n\nconst requestBody = {\n  systemInstruction: {\n    role: 'system',\n    parts: [{ text: j.systemInstructionText || '' }],\n  },\n  contents: [{ role: 'user', parts }],\n  generationConfig: j.generationConfig || {},\n  ...(j.model ? { model: j.model } : {}),\n};\n\nreturn {\n  json: {\n    ...j,\n    requestBody,\n    requestBodyMeta: {\n      source: 'repo-w2a-base-input@v1',\n      hasHairRef: !!(b.hair && j.hairRefS3Key),\n      hasSkinSwatch: !!(b.skin && j.promptMeta?.hasSkinSwatch),\n      baseBinaryKey: baseKey,\n    },\n  },\n  binary: b,\n  pairedItem: ($input && $input.item && $input.item.pairedItem) ? $input.item.pairedItem : { item: itemIx },\n};"
      },
      "id": "5314b26f-9280-4c9e-a96b-4520a1f3f108",
      "name": "Build Repo Gemini Base Request",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        448,
        112
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 1
          },
          "conditions": [
            {
              "id": "95044160-ed49-4a23-8c82-b8fde9a85f67",
              "leftValue": "={{ !!($json.__testMode || $json.testMode || $json.__meta?.__testMode || $json.ctx?.__testMode || $json.ctx?.testMode) }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "4f0c6142-3a09-41f3-b234-586475274c24",
      "name": "IF: Test Mode? (1)1",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [
        672,
        112
      ],
      "notes": "Routes to MOCK (true) or REAL API (false)"
    },
    {
      "parameters": {
        "jsCode": "// 🧪 MOCK: Gemini Image Generation — EACH mode\n// Emits a realistic Gemini response with a tiny 1x1 PNG.\n// Sets flags so downstream extractor accepts tiny images in test mode.\n\nconst j = $json || {};\nconst itemIx = typeof $itemIndex === 'number' ? $itemIndex : 0;\n\n// 1×1 transparent PNG (base64)\nconst TINY_PNG_BASE64 =\n  \"iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg==\";\n\n// Choose response shape: 'top' (default) or 'body' to simulate HTTP wrappers\nconst shape = j.__mockShape === 'body' ? 'body' : 'top';\n\nconst payload = {\n  candidates: [{\n    content: {\n      parts: [{\n        inlineData: {\n          mimeType: \"image/png\",\n          data: TINY_PNG_BASE64\n        }\n      }]\n    },\n    finishReason: \"STOP\",\n    safetyRatings: []\n  }],\n  usageMetadata: {\n    promptTokenCount: 100,\n    candidatesTokenCount: 50,\n    totalTokenCount: 150\n  },\n  modelVersion: \"gemini-2.5-flash-image-mock\"\n};\n\n// Wrap under body if requested (lets you test extractor's pickRoot logic)\nconst mockResponse = shape === 'body' ? { body: payload } : payload;\n\n// Mark this as a mock so the extractor relaxes size checks\nmockResponse.modelVersion = mockResponse.modelVersion || \"gemini-2.5-flash-image-mock\";\nmockResponse.__testMode = true;\nmockResponse.__allowTinyImage = true;\n// Optional: force a very low size gate in case flags aren’t seen\nmockResponse.minImageBytes = 1;\n\nconsole.log('🧪 MOCK: Returning fake image for base/pose',\n  j.poseNumber ?? j.currentPoseNumber ?? '?', 'shape=', shape);\n\nreturn {\n  json: { ...($json || {}), ...mockResponse },\n  binary: $binary || {},\n  pairedItem: ($input && $input.item && $input.item.pairedItem) ? $input.item.pairedItem : { item: itemIx }\n};\n"
      },
      "id": "134f79c4-df90-4d4d-b0eb-5f9793df8b4a",
      "name": "🧪 MOCK: Generate Custom Base Character1",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        896,
        0
      ],
      "notes": "Mock response for testing - no API calls made"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://generativelanguage.googleapis.com/v1beta/models/gemini-3-pro-image-preview:generateContent",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "googlePalmApi",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Content-Type",
              "value": "application/json"
            },
            {
              "name": "X-Client-Request-Id",
              "value": "={{$json.correlationId}}"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ $json.requestBody }}",
        "options": {
          "timeout": 180000
        }
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        896,
        224
      ],
      "id": "c8d7f8b8-437d-45ed-9075-3445399129b0",
      "name": "Generate Custom Base Character1",
      "credentials": {
        "googlePalmApi": {
          "id": "7jdcfc9T2O9vensv",
          "name": "Google Gemini(PaLM) Api account"
        }
      },
      "onError": "continueRegularOutput"
    },
    {
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "// Extract BASE CHARACTER image from Gemini response — repo-centric SW0\nconst apiJson = $json || {};\nconst b = $binary || {};\nconst itemIx = (typeof $itemIndex === 'number') ? $itemIndex : 0;\n\nfunction safeItems(name, runIx) {\n  try { return $items(name, 0, runIx) || $items(name) || []; } catch { return []; }\n}\nconst runIx = (typeof $runIndex === 'number') ? $runIndex : 0;\nconst upstreamCandidates = [\n  'Build Repo Gemini Base Request',\n  'Schema Check + Defaults',\n  'Fetch Repo W2A Base Input'\n];\n\nlet upstream = {};\nfor (const n of upstreamCandidates) {\n  const now = safeItems(n, runIx);\n  const seed = safeItems(n, 0);\n  upstream = (now[itemIx]?.json) || (seed[itemIx]?.json) || now[0]?.json || seed[0]?.json || upstream;\n  if (upstream && Object.keys(upstream).length) break;\n}\nupstream = upstream || {};\n\nconst pick = (...v) => v.find(x => x !== undefined && x !== null && x !== '');\n\nfunction pickRoot(r){\n  if (r?.body?.candidates) return r.body;\n  if (r?.response?.candidates) return r.response;\n  if (r?.data?.candidates) return r.data;\n  return r;\n}\n\nfunction firstInlineFromParts(parts){\n  if (!Array.isArray(parts)) return null;\n  for (const p of parts){\n    const id = p?.inlineData?.data || p?.inline_data?.data;\n    if (id) {\n      return {\n        b64: String(id),\n        mime: p?.inlineData?.mimeType || p?.inline_data?.mime_type || 'image/png',\n        path: 'content.parts.inlineData'\n      };\n    }\n  }\n  return null;\n}\n\nfunction findInlineImage(r){\n  const cand = r?.candidates?.[0];\n  if (cand?.content?.parts){\n    const got = firstInlineFromParts(cand.content.parts);\n    if (got) return got;\n  }\n  if (Array.isArray(cand?.content) && cand.content[0]?.parts){\n    const got = firstInlineFromParts(cand.content[0].parts);\n    if (got) return got;\n  }\n  const stack = [r];\n  while (stack.length) {\n    const cur = stack.pop();\n    if (!cur || typeof cur !== 'object') continue;\n    const d = cur?.inlineData?.data || cur?.inline_data?.data;\n    if (d) {\n      return {\n        b64: String(d),\n        mime: cur?.inlineData?.mimeType || cur?.inline_data?.mime_type || 'image/png',\n        path: '...inlineData/inline_data (fallback)'\n      };\n    }\n    for (const k of Object.keys(cur)) {\n      const v = cur[k];\n      if (v && typeof v === 'object') stack.push(v);\n    }\n  }\n  return null;\n}\n\nfunction fnv1aHex(buf){\n  let h = 0x811c9dc5 >>> 0;\n  for (let i = 0; i < buf.length; i++){\n    h ^= buf[i];\n    h = Math.imul(h, 0x01000193) >>> 0;\n  }\n  return ('00000000' + h.toString(16)).slice(-8);\n}\n\nconst resp = pickRoot(apiJson);\nconst hit = findInlineImage(resp);\nconst finish = resp?.candidates?.[0]?.finishReason\n  || resp?.promptFeedback\n  || resp?.candidates?.[0]?.safetyRatings\n  || null;\nconst finishBrief = (typeof finish === 'string')\n  ? finish\n  : JSON.stringify(finish || {}, null, 0).slice(0, 300);\n\nconst isTest = !!(\n  upstream.__testMode || upstream.testMode ||\n  upstream.__meta?.__testMode || upstream.ctx?.__testMode || upstream.ctx?.testMode\n);\n\nif (!hit || !hit.b64) {\n  throw new Error(`No inline image in Gemini response. finish/safety: ${finishBrief || 'n/a'}`);\n}\n\nconst buf = Buffer.from(hit.b64, 'base64');\nconst mime = hit.mime || 'image/png';\nconst minBytes = Number.isFinite(Number(upstream.minImageBytes)) ? Number(upstream.minImageBytes) : 40;\nif (!isTest && buf.length < minBytes) {\n  throw new Error(`Inline image too small (${buf.length}B < ${minBytes}B). finish/safety: ${finishBrief || 'n/a'}`);\n}\n\nlet characterHash = pick(\n  upstream.characterHash,\n  upstream.ctx?.characterHash,\n  upstream.__meta?.characterHash,\n  null,\n);\nif (!characterHash) {\n  const slug = isTest\n    ? String(upstream.amazonOrderId || upstream.orderId || 'test').toLowerCase().replace(/[^a-z0-9]/g,'').slice(0,12)\n    : `img${fnv1aHex(buf)}`;\n  characterHash = slug || 'test';\n}\n\nconst fileName = upstream.generatedFileName || `characters_${characterHash}_base.png`;\nconst genBin = await this.helpers.prepareBinaryData(buf, fileName, mime);\ngenBin.mimeType = mime;\ngenBin.fileExtension = (mime.split('/')[1] || 'png').toLowerCase();\n\nlet hairMeta = upstream.hairPromptMeta;\nif (typeof hairMeta === 'string') {\n  try { hairMeta = JSON.parse(hairMeta); } catch { hairMeta = {}; }\n}\nif (!hairMeta || typeof hairMeta !== 'object') hairMeta = {};\n\nreturn {\n  json: {\n    ...upstream,\n    characterHash,\n    ctx: { ...(upstream.ctx || {}), characterHash },\n    generatedImage: {\n      mimeType: mime,\n      size: buf.length,\n      isMock: isTest,\n      modelVersion: resp?.modelVersion || null,\n    },\n    baseGeneratedFileName: fileName,\n    hairPromptMeta: hairMeta,\n    __meta: {\n      ...(upstream.__meta || {}),\n      generatedAttemptKey: fileName,\n      sourcePath: hit.path,\n      extractGenerated: { source: b?.data?.data ? 'binary' : 'json' },\n      finishBrief,\n    },\n  },\n  binary: { ...b, data: genBin },\n};"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1120,
        112
      ],
      "id": "61e10c38-4fba-4019-ba36-640aef230995",
      "name": "Process Gemini API response and extract generated image1",
      "onError": "continueRegularOutput"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{(($json.backendUrl || $json.orderContext?.backendUrl || 'https://admin.littleherolabs.com').replace(/\\/$/, '')) + '/api/internal/w2a/finalize-base-result'}}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Authorization",
              "value": "=Bearer REDACTED_BACKEND_API_TOKEN"
            },
            {
              "name": "Content-Type",
              "value": "application/json"
            }
          ]
        },
        "sendBody": true,
        "contentType": "raw",
        "rawContentType": "application/json",
        "body": "={{JSON.stringify($json)}}",
        "options": {
          "response": {
            "response": {
              "responseFormat": "json"
            }
          },
          "timeout": 90000
        }
      },
      "id": "b8098b90-0a47-4659-960a-4b8cae8d84d2",
      "name": "Finalize Repo W2A Base Result",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1344,
        112
      ]
    },
    {
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "\nfunction asObject(value) {\n  return value && typeof value === 'object' && !Array.isArray(value) ? value : {};\n}\n\nfunction toBufferLike(value) {\n  if (!value) return null;\n  if (Buffer.isBuffer(value)) return value;\n  if (typeof value === 'string') return Buffer.from(value, 'utf8');\n  if (Array.isArray(value)) return Buffer.from(value);\n  const record = asObject(value);\n  if (record.type === 'Buffer' && Array.isArray(record.data)) return Buffer.from(record.data);\n  if (Array.isArray(record.data)) return Buffer.from(record.data);\n  return null;\n}\n\nfunction extractStreamText(value) {\n  const source = asObject(value);\n  const directOutBuffer = toBufferLike(source._outBuffer);\n  const outOffset = typeof source._outOffset === 'number' ? source._outOffset : null;\n  if (directOutBuffer && directOutBuffer.length > 0) {\n    const end = outOffset !== null && outOffset >= 0\n      ? Math.min(outOffset, directOutBuffer.length)\n      : directOutBuffer.length;\n    const text = directOutBuffer.subarray(0, end).toString('utf8');\n    if (text) return text;\n  }\n\n  const readableState = asObject(source._readableState);\n  const chunks = [];\n  const pushChunk = (chunk) => {\n    const buffer = toBufferLike(chunk);\n    if (buffer && buffer.length > 0) chunks.push(buffer);\n  };\n\n  const bufferState = readableState.buffer;\n  if (Array.isArray(bufferState)) {\n    for (const entry of bufferState) {\n      const record = asObject(entry);\n      pushChunk(entry);\n      pushChunk(record.data);\n      pushChunk(record.chunk);\n    }\n  } else {\n    const bufferList = asObject(bufferState);\n    let current = bufferList.head || null;\n    let guard = 0;\n    while (current && guard < 10000) {\n      const record = asObject(current);\n      pushChunk(record.data);\n      pushChunk(record.chunk);\n      current = record.next || null;\n      guard += 1;\n    }\n  }\n\n  if (!chunks.length) return null;\n  return Buffer.concat(chunks).toString('utf8');\n}\n\nfunction parseJsonPrefix(text) {\n  if (typeof text !== 'string') return null;\n  const start = text.search(/[\\[{]/);\n  if (start === -1) return null;\n\n  const stack = [];\n  let inString = false;\n  let escape = false;\n\n  for (let index = start; index < text.length; index += 1) {\n    const char = text[index];\n\n    if (inString) {\n      if (escape) {\n        escape = false;\n      } else if (char === '\\\\') {\n        escape = true;\n      } else if (char === '\"') {\n        inString = false;\n      }\n      continue;\n    }\n\n    if (char === '\"') {\n      inString = true;\n      continue;\n    }\n\n    if (char === '{' || char === '[') {\n      stack.push(char);\n      continue;\n    }\n\n    if (char === '}' || char === ']') {\n      const expected = char === '}' ? '{' : '[';\n      if (!stack.length || stack[stack.length - 1] !== expected) return null;\n      stack.pop();\n      if (!stack.length) {\n        try {\n          return JSON.parse(text.slice(start, index + 1));\n        } catch {\n          return null;\n        }\n      }\n    }\n  }\n\n  return null;\n}\n\nfunction parseMaybeJson(value) {\n  if (typeof value === 'string') {\n    try { return JSON.parse(value); } catch { return parseJsonPrefix(value) ?? value; }\n  }\n\n  const streamText = extractStreamText(value);\n  if (typeof streamText === 'string') {\n    try { return JSON.parse(streamText); } catch { return parseJsonPrefix(streamText) ?? value; }\n  }\n\n  return value;\n}\n\nfunction unwrapResponse(value) {\n  const parsed = parseMaybeJson(value);\n  const obj = asObject(parsed);\n  if (obj.success === true || obj.baseCharacterKey || obj.characterHash) {\n    return obj;\n  }\n  for (const candidate of [obj.body, obj.data, obj.response, obj.result]) {\n    const nested = asObject(parseMaybeJson(candidate));\n    if (nested.success === true || nested.baseCharacterKey || nested.characterHash) {\n      return nested;\n    }\n  }\n  return obj;\n}\n\nconst itemIx = (typeof $itemIndex === 'number') ? $itemIndex : 0;\nconst runIx = (typeof $runIndex === 'number') ? $runIndex : 0;\nlet extracted = null;\ntry {\n  const items = $items('Process Gemini API response and extract generated image1', 0, runIx) || [];\n  extracted = items[itemIx] || items[0] || null;\n} catch {}\n\nconst payload = unwrapResponse($json || {});\nif (!payload || typeof payload !== 'object' || Array.isArray(payload)) {\n  throw new Error('Could not parse finalized W2A base result');\n}\nif (payload.error && !payload.baseCharacterKey && !payload.characterHash) {\n  throw new Error(payload.details || payload.error);\n}\n\nreturn {\n  json: payload,\n  binary: extracted?.binary || $binary || {},\n  pairedItem: ($input?.item?.pairedItem) ? $input.item.pairedItem : (extracted?.pairedItem || { item: itemIx }),\n};"
      },
      "id": "cf544db1-a0a3-4b8e-9005-9ef9ac1fbc69",
      "name": "Restore Finalized Base Envelope",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1568,
        112
      ]
    },
    {
      "parameters": {
        "operation": "upload",
        "bucketName": "little-hero-assets",
        "fileName": "={{ $json.__meta.storageKey }}",
        "additionalFields": {}
      },
      "type": "n8n-nodes-base.s3",
      "typeVersion": 1,
      "position": [
        1792,
        112
      ],
      "id": "bcf06207-7777-4002-850f-e6b53eaf3568",
      "name": "Upload a file1",
      "credentials": {
        "s3": {
          "id": "7tJOX9QjL1jqyEjf",
          "name": "S3 account"
        }
      }
    },
    {
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "\nconst itemIx = (typeof $itemIndex === 'number') ? $itemIndex : 0;\nconst runIx = (typeof $runIndex === 'number') ? $runIndex : 0;\nlet preserved = null;\ntry {\n  const items = $items('Restore Finalized Base Envelope', 0, runIx) || [];\n  preserved = items[itemIx] || items[0] || null;\n} catch {}\nif (!preserved?.json) {\n  throw new Error('Restore Uploaded Base Envelope could not rehydrate finalized base envelope');\n}\nreturn {\n  json: preserved.json,\n  binary: {},\n  pairedItem: preserved.pairedItem || { item: itemIx },\n};"
      },
      "id": "9f8c98fb-6f0a-43c2-927c-4ac59701b53a",
      "name": "Restore Uploaded Base Envelope",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2016,
        112
      ]
    },
    {
      "parameters": {
        "assignments": {
          "assignments": [
            {
              "id": "45f3a79d-0a00-4559-9ccb-4ab091b619f0",
              "name": "characterHash",
              "value": "={{ $json.characterHash || $json.ctx?.characterHash }}",
              "type": "string"
            },
            {
              "id": "43bf4416-ae70-4e75-9f32-33170954c2c1",
              "name": "=characterPath",
              "value": "={{ $json.characterPath || ('characters/' + ($json.characterHash || $json.ctx?.characterHash)) }}",
              "type": "string"
            },
            {
              "id": "56cb132d-54e5-419b-bafd-9b6ff31ad486",
              "name": "publicR2Url",
              "value": "={{ $json.publicR2Url }}",
              "type": "string"
            },
            {
              "id": "0413727b-8300-48fe-988b-544b41b8eb98",
              "name": "skinToneCanonical",
              "value": "={{ $json.skinToneCanonical\n    || $json.characterSpecs?.skinToneCanonical\n    || $json.characterSpecs?.skinTone\n    || 'skin-medium' }}",
              "type": "string"
            },
            {
              "id": "c289d626-6f95-4797-b6a1-63be96d67a3e",
              "name": "clothingTypeCanonical",
              "value": "={{ $json.clothingTypeCanonical }}",
              "type": "string"
            },
            {
              "id": "b6bbb1dd-2a21-4fed-80c0-7c9a2f95e0f4",
              "name": "=hairStyleCanonical",
              "value": "={{ $json.hairStyleCanonical }}",
              "type": "string"
            },
            {
              "id": "5a176e67-f982-4a98-a0f5-b09da8a3b5db",
              "name": "=hairPromptMeta",
              "value": "={{ $json.hairPromptMeta }}",
              "type": "json"
            },
            {
              "id": "6fb811f1-4360-4398-acd3-588990187010",
              "name": "base.uploadKey",
              "value": "={{ $json.base?.uploadKey || $json.__meta?.storageKey || $json.generatedUploadKey || $json.uploadResult?.key || $json.uploadResult?.Key }}",
              "type": "string"
            },
            {
              "id": "d7f098fb-1758-4ed2-a61b-58c1cd850ff4",
              "name": "base.publicUrl",
              "value": "={{ $json.base?.publicUrl || ( $json.publicR2Url && ($json.base?.uploadKey || $json.__meta?.storageKey || $json.generatedUploadKey) ? $json.publicR2Url + '/' + ($json.base?.uploadKey || $json.__meta?.storageKey || $json.generatedUploadKey) : null ) }}",
              "type": "string"
            },
            {
              "id": "87fcb938-b7c0-4fa5-8ce1-c2e3dbf1ed7d",
              "name": "base.mime",
              "value": "={{ $json.base?.mime || $json.generatedImage?.mimeType || 'image/png' }}",
              "type": "string"
            },
            {
              "id": "3b04407d-2c3c-48bb-9322-26045fafeff2",
              "name": "__testMode",
              "value": "={{ $json.__testMode }}",
              "type": "boolean"
            },
            {
              "id": "561d4d56-50b6-4452-ba93-c89914624f36",
              "name": "__meta.sw0Completed",
              "value": "={{ true }}",
              "type": "boolean"
            },
            {
              "id": "5aafe855-151b-4760-9473-f0bc23ce2b5c",
              "name": "generatedBytes",
              "value": "={{ $json.base?.bytes ?? $json.__meta?.bytes ?? $json.generatedImage?.size ?? null }}",
              "type": "number"
            },
            {
              "id": "faa5a91c-4b20-4ac9-94bf-4595ef852987",
              "name": "baseCharacterKey",
              "value": "={{ $json.baseCharacterKey }}",
              "type": "string"
            },
            {
              "id": "8fededa1-d197-4a98-8063-08daede2a5ed",
              "name": "basePublicUrl",
              "value": "={{ $json.baseRefPublicUrl }}",
              "type": "string"
            },
            {
              "id": "e393472f-b1d7-4182-8f0a-611158439e54",
              "name": "hairRefS3Key",
              "value": "={{ $json.hairRefS3Key }}",
              "type": "string"
            },
            {
              "id": "7fafe0af-71a0-4328-a9f3-5ac8150ed98c",
              "name": "hairRefPublicUrl",
              "value": "={{ $json.hairRefPublicUrl }}",
              "type": "string"
            }
          ]
        },
        "includeOtherFields": true,
        "options": {
          "dotNotation": true
        }
      },
      "id": "f460e4f6-04e1-4e64-91e9-5d07e49cb42d",
      "name": "Return Generated Image1",
      "type": "n8n-nodes-base.set",
      "typeVersion": 3.4,
      "position": [
        2240,
        112
      ],
      "notesInFlow": true,
      "notes": "Returns envelope with bin.generated and slimmed payload"
    }
  ],
  "connections": {
    "When Executed by Another Workflow": {
      "main": [
        [
          {
            "node": "Fetch Repo W2A Base Input",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Fetch Repo W2A Base Input": {
      "main": [
        [
          {
            "node": "Schema Check + Defaults",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Schema Check + Defaults": {
      "main": [
        [
          {
            "node": "Load Base Character Image1",
            "type": "main",
            "index": 0
          },
          {
            "node": "Load Hairstyle Reference (R2/S3)1",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Load Base Character Image1": {
      "main": [
        [
          {
            "node": "Merge Base & Hair Refs1",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Load Hairstyle Reference (R2/S3)1": {
      "main": [
        [
          {
            "node": "Merge Base & Hair Refs1",
            "type": "main",
            "index": 1
          }
        ]
      ]
    },
    "Merge Base & Hair Refs1": {
      "main": [
        [
          {
            "node": "Build Repo Gemini Base Request",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Repo Gemini Base Request": {
      "main": [
        [
          {
            "node": "IF: Test Mode? (1)1",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "IF: Test Mode? (1)1": {
      "main": [
        [
          {
            "node": "🧪 MOCK: Generate Custom Base Character1",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Generate Custom Base Character1",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "🧪 MOCK: Generate Custom Base Character1": {
      "main": [
        [
          {
            "node": "Process Gemini API response and extract generated image1",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Generate Custom Base Character1": {
      "main": [
        [
          {
            "node": "Process Gemini API response and extract generated image1",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Process Gemini API response and extract generated image1": {
      "main": [
        [
          {
            "node": "Finalize Repo W2A Base Result",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Finalize Repo W2A Base Result": {
      "main": [
        [
          {
            "node": "Restore Finalized Base Envelope",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Restore Finalized Base Envelope": {
      "main": [
        [
          {
            "node": "Upload a file1",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Upload a file1": {
      "main": [
        [
          {
            "node": "Restore Uploaded Base Envelope",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Restore Uploaded Base Envelope": {
      "main": [
        [
          {
            "node": "Return Generated Image1",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1",
    "callerPolicy": "workflowsFromSameOwner",
    "availableInMCP": false
  },
  "versionId": "57d6c25d-0860-46bc-9edd-81d1021fb006",
  "meta": null,
  "id": "S5apnAgeuitSIO5Z",
  "tags": []
}
