---
interface Props {
  currentStep: 'character' | 'customize' | 'checkout' | 'processing';
}

const { currentStep } = Astro.props;

const steps: { id: 'character' | 'customize' | 'checkout' | 'processing'; label: string }[] = [
  { id: 'character', label: 'Character' },
  { id: 'customize', label: 'Customize' },
  { id: 'checkout', label: 'Checkout' },
  { id: 'processing', label: 'Processing' },
];
---

<nav class="step-indicator" aria-label="Create your book progress">
  <ol class="step-list" role="list">
    {steps.map((step) => (
      <li
        class:list={['step-item', step.id === currentStep && 'step-item--current']}
        aria-current={step.id === currentStep ? 'step' : undefined}
      >
        <span class="step-label">{step.label}</span>
      </li>
    ))}
  </ol>
</nav>

<style>
  .step-indicator {
    padding: var(--spacing-md, 1rem) 0;
  }

  .step-list {
    display: flex;
    flex-wrap: wrap;
    gap: var(--spacing-sm, 0.5rem);
    list-style: none;
    margin: 0;
    padding: 0;
    justify-content: center;
    align-items: center;
  }

  .step-item {
    display: flex;
    align-items: center;
    font-family: var(--font-ui);
    font-size: 0.875rem;
    color: var(--color-soft-charcoal, #333);
  }

  .step-item--current .step-label {
    font-weight: 600;
    color: var(--color-navy, #2D3142);
  }

  .step-item:not(:last-child)::after {
    content: '›';
    margin-left: var(--spacing-sm, 0.5rem);
    margin-right: var(--spacing-sm, 0.5rem);
    color: var(--color-teal, #5AC6B1);
  }

  .step-label {
    color: inherit;
  }
</style>
